<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Детали URL</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            margin: 0;
            padding: 20px;
        }
        .container {
            max-width: 800px;
            margin: 0 auto;
        }
        h1 {
            color: #333;
        }
        .flash-message {
            padding: 10px;
            margin-bottom: 20px;
            border-radius: 4px;
        }
        .flash-success {
            background-color: #d4edda;
            color: #155724;
            border: 1px solid #c3e6cb;
        }
        .flash-error {
            background-color: #f8d7da;
            color: #721c24;
            border: 1px solid #f5c6cb;
        }
        .url-details {
            margin-top: 20px;
        }
        .url-details dt {
            font-weight: bold;
            margin-top: 10px;
        }
        .url-details dd {
            margin-bottom: 10px;
        }
        a {
            color: #007bff;
            text-decoration: none;
        }
        a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Детали URL</h1>

        <!-- Вывод flash-сообщений -->
        <?php if (!empty($flash)): ?>
            <?php foreach ($flash as $type => $messages): ?>
                <?php foreach ($messages as $message): ?>
                    <div class="flash-message flash-<?= htmlspecialchars($type) ?>">
                        <?= htmlspecialchars($message) ?>
                    </div>
                <?php endforeach; ?>
            <?php endforeach; ?>
        <?php endif; ?>

        <!-- Детали URL -->
        <div class="url-details">
            <dl>
                <dt>ID:</dt>
                <dd><?= htmlspecialchars($url->getId()) ?></dd>

                <dt>URL:</dt>
                <dd><a href="<?= htmlspecialchars($url->getName()) ?>" target="_blank"><?= htmlspecialchars($url->getName()) ?></a></dd>

                <dt>Дата создания:</dt>
                <dd><?= htmlspecialchars($url->getCreatedAt()) ?></dd>
            </dl>

            <!-- Ссылка на список URL -->
            <p><a href="/urls">← Вернуться к списку URL</a></p>
        </div>
    </div>
</body>
</html>